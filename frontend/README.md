# NovaFlow Frontend

This folder contains the Vite + React UI for NovaFlow AI.

Run it from the repo root with:

```bash
pnpm dev
```

Or from inside this folder with:

```bash
pnpm dev
pnpm build
pnpm lint
```
